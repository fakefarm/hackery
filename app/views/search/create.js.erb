var search = <%= "#{@search}" %>
$.ajax({
  url: 'https://jobs.github.com/positions.json?search=ruby',
  dataType: 'jsonp',
  success: function(data) {
    console.log(data);
    $('.element').html("searched for: " + search + ": " + data[0].company)
  }
});
